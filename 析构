和默认构造函数一样(空参数，空函数体)，也存在默认析构函数，同样，编译器也会为类生成“合成的默认析构函数”。

默认析构函数的默认动作(函数体是空的)会将析构当前实例。同时，在销毁自己之前，还会销毁其父类实例，爷爷类也
是。

注：这里需要说明，子类的创建流程也是先创建爷爷类，再父类，最后再自己。

问题：是不是创建子类实例 就 必须要创建父类实例。那么使用继承默认的话，会凭空消耗很多不必要的内存？？？
