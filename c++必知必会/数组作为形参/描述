数组作为形参有如下几种情况：

1） void myfunc(int a[])		//传入的是数组的首地址，和void myfunc(int *a)没区别
2） void myfunc(int a[10])		//传入的是数组的首地址，“但是要求数组的个数必须是10个”
3） void myfunc(int a[]，int size)	//传入的是数组的首地址，但是要求指定数组个数，非强制

PS:多维数组类似

（*）一般使用的时候用2）来做语法上的强制要求，然后用3）做实现以增加灵活度




但是更推荐使用Array类和其他容器



